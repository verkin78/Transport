package Transport;

/*public class Driver<B extends Car, D extends Bus, C extends Freight> {
    private String fullName;
    private boolean driveLicense;
    private int experience;
    private DriverLicense driverLicense;
    private String driver;

    public Driver(String fullName, boolean driveLicense, int experience, DriverLicense driverLicense,
                  String driver) {
        this.fullName = fullName;
        this.driveLicense = driveLicense;
        this.experience = experience;
        this.driverLicense = driverLicense;
        this.driver = driver;
    }

    public String getFullName() {
        return fullName;
    }

    public void setFullName(String fullName) {
        this.fullName = fullName;
    }

    public boolean isDriveLicense(DriverLicense) {
        return driveLicense;
    }

    public void setDriveLicense(boolean driveLicense) {
        this.driveLicense = driveLicense;
    }

    public int getExperience() {
        return experience;
    }

    public void setExperience(int experience) {
        this.experience = experience;
    }

    public void setDriverLicense(DriverLicense driverLicense) {
        if (driverLicense != DriverLicense.DRIVER_B) {
            throw new IllegalArgumentException("Некорректные данные");
        } else {
            if (driverLicense != DriverLicense.DRIVER_C)
                throw new IllegalArgumentException("Некорректные данные");
        } else{
            if (driverLicense != DriverLicense.DRIVER_D)
                throw new IllegalArgumentException("Некорректные данные");
        } else{
            this.driverLicense = driverLicense;
        }
    }

    public String getDriver() {
        return driver;
    }

    public void setDriver(String driver) {
        this.driver = driver;
    }

    @Override
    public String toString() {
        return "Driver{" +
                "fullName='" + fullName + '\'' +
                ", experience=" + experience +
                ", driverLicense=" + driverLicense +
                '}';
    }

    public void refill() {
        System.out.println("Машина заправляется.");
    }

    public void moveStart() {
        System.out.println("Водитель " + getFullName() + " на машине" + Transport.getBrand() +
                Transport.getModel() + "готов" +
                " участвовать в гонках.");
        Transport.printType(<>);
        moveStop();
        refill();
    }

    public void moveStop() {
        System.out.println("Водитель останавливается.");
    }

    public Driver<>()

    {

    }

    @Override
    public int hashCode() {
        return super.hashCode();
    }
}*/
